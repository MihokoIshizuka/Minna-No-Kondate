<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'info', member: @member %>
    </div>
    <div class="col-md-9">
      <div class="row justify-content-center">
        <div class="col-md-6">
          <%= render 'public/searches/search' %>
        </div>
        <!--献立タグ絞り込み-->
        <div class= "col-md-4 search-tag">
          <%= form_with url: member_path(@member), local: true, method: :get do |f| %>
            <%= select_tag :tag_id, options_from_collection_for_select(Tag.all, :id, :name, params[:tag_id]),
            {
              prompt: 'タグで絞り込み',
              class: "form-control tags-select",
              onchange: 'submit(this.form);'
            }
            %>
          <% end %>
        </div>
      </div>
      <!--タブメニュー-->
      <ul class="tab-list list-unstyled">
        <li class="tab tab-active">朝</li>
        <li class="tab">昼</li>
        <li class="tab">夕</li>
        <li class="tab">間食</li>
      </ul>
      <div class="tabbox-contents">
        <div class="tabbox box-show">
          <%= render 'public/menus/index', menus: @morning_menus %>
        </div>
        <div class="tabbox">
          <%= render 'public/menus/index', menus: @noon_menus %>
        </div>
        <div class="tabbox">
          <%= render 'public/menus/index', menus: @evening_menus %>
        </div>
        <div class="tabbox">
          <%= render 'public/menus/index', menus: @snack_menus %>
        </div>
      </div>
    </div>
  </div>
</div>
<span id="new">
  <%= link_to new_menu_path, class: "btn" do %>
    <i class="fas fa-plus-circle fa-5x new-btn" style="color:steelblue;"></i>
  <% end %>
</span>
