<p>コメント件数: <%= menu.menu_comments.count %> 件</p>
<% menu.menu_comments.each do |menu_comment| %>
  <div class="row">
    <div class="col-2">
      <% unless menu_comment.member_id.nil? %>
        <%= link_to admin_member_path(menu_comment.member) do %>
          <%= image_tag menu_comment.member.get_profile_image, size:"65x65" %><br>
          <%= menu_comment.member.name %>
        <% end %>
      <% else %>
        <%= image_tag 'admin.png', size:"65x65" %>
      <% end %>
    </div>
    <div class="col-4">
      <!--コメント改行表示できる-->
      <%=safe_join(menu_comment.comment.split("\n"),tag(:br))%><br>
      <%= menu_comment.created_at. strftime('%Y/%m/%d') %>
      <%= link_to "削除", admin_menu_menu_comment_path(menu_comment.menu, menu_comment), method: :delete, remote: true, class: "btn btn-sm btn-danger" %>
    </div>
  </div>
<% end %>